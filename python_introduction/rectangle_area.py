# Assigning values to the length and width of the rectangle
length = 10     # length of the rectangle
width = 5       # width of the rectangle

# Calculating the area using the formula: Area = length * width
area = length * width

# Printing the area in a human-readable format
print(f"The area of the rectangle is: {area}")